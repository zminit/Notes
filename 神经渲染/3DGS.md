# 3DGS

## 前置基础

**splatting**


**高斯椭球**

$$G(x)=\frac{1}{\sqrt{(2\pi)^k|\Sigma|}}e^{-\frac{1}{2}(x-\mu)^T\Sigma^{-1}(x-\mu)}$$

- $\mu$ 是均值向量，$\Sigma$ 是协方差矩阵, 半正定，$k$ 是维度。

- 仿射变换高斯核仍然闭合。
- 高斯核沿着某一个轴积分降维后，仍然是高斯。
- 高斯核是一个实心椭球，每一层代表一个概率。
- 协方差矩阵可以使用旋转和缩放矩阵表示。 $\Sigma = RSS^T R^T$
