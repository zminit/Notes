- 什么是可微渲染？
    >可微渲染是渲染过程对渲染参数可微分的渲染。比如传统渲染中，光线可见度函数在部分区域是1，部分区域是0，中间的跳变不可微。而可微渲染，如体渲染，渲染过程只使用了加/减/乘/除，可以进行微分。

- 什么是可微渲染器？
    > 可微渲染器是指在渲染过程中，不仅能把场景参数渲染成图像，还能计算图像对场景参数的梯度。

- 传统渲染为什么不可微？

- 神经BRDF
- VGGT
- 神经纹理